// Generated by CoffeeScript 1.6.3
(function() {
  $.fn.indicate = function(resultBool) {
    var color,
      _this = this;
    color = resultBool ? '#090' : '#900';
    this.css('border-color', color);
    return setTimeout(function() {
      return _this.css('border-color', 'transparent');
    }, 1600);
  };

  $(function() {
    $('a.push').on('click', function(e) {
      var listItem, request, target;
      e.preventDefault();
      target = $(e.currentTarget);
      listItem = target.parent();
      request = $.ajax({
        type: 'PUT',
        url: "" + window.location.pathname + "/language/" + (target.data('language')) + "/push"
      });
      request.done(function() {
        return listItem.indicate(true);
      });
      return request.fail(function() {
        return listItem.indicate(false);
      });
    });
    return $('form.generate').on('submit', function(e) {
      var request;
      e.preventDefault();
      request = $.ajax({
        type: 'PUT',
        url: "" + window.location.pathname + "/generate",
        data: $(this).serializeArray()
      });
      request.done(function() {
        return console.log('generate success');
      });
      return request.fail(function() {
        return console.log('generate fail');
      });
    });
  });

}).call(this);
